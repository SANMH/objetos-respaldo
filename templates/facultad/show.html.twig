{% extends 'base.html.twig' %}

{% block title %}Facultad{% endblock %}

{% block body %}
    <h1>Facultad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facultad.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ facultad.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('facultad_index') }}">back to list</a>

    <a href="{{ path('facultad_edit', {'id': facultad.id}) }}">edit</a>

    {{ include('facultad/_delete_form.html.twig') }}
{% endblock %}
