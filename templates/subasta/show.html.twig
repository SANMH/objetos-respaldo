{% extends 'base.html.twig' %}

{% block title %}Subasta{% endblock %}

{% block body %}
    <h1>Subasta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Objeto</th>
                <td>{{ subastum.objetos.name }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ subastum.valor }}</td>
            </tr>
            <tr>
                <th>FechaIngreso</th>
                <td>{{ subastum.fechaIngreso ? subastum.fechaIngreso|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subasta_index') }}"><button type="button" class="btn btn-secondary btn-lg">Regresar a la lista</button></a>

    <a href="{{ path('subasta_edit', {'id': subastum.id}) }}"><button type="button" class="btn btn-success">editar</button></a>

    {{ include('subasta/_delete_form.html.twig') }}
{% endblock %}
